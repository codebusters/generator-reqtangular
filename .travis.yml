language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-reqtangular' ]; then cd .. && eval "mv $currentfolder generator-reqtangular" && cd generator-reqtangular; fi

